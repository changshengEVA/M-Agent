# Optional Neo4j integration. Credentials must never be committed here.
url: "bolt://127.0.0.1:7687"
user_name: "neo4j"
password_env: "NEO4J_PASSWORD"

# Workflow database routing (always isolated per workflow):
database_template: "wf-{workflow_id}"
